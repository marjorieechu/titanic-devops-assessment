apiVersion: v1
kind: Secret
metadata:
  name: {{ include "titanic-api.fullname" . }}-secret
  labels:
    {{- include "titanic-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
